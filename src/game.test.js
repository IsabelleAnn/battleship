import { test, expect } from "jest";
import * as game from "game.js";

//test if coords length matches length of ship
// test("increase number of hits in ship", () => {
//   expect(ship5.hits).toBe(1);
// });

//test if coords are consecutive for row

//test if coords are consecutive for col

//test if coords stay on grid
